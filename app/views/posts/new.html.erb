<h2>New post</h2>
<%= form_with model: @post, url: new_post_path(@current_user.id) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :text %>
  <%= form.text_area :text %>
  <%= form.submit %>
<% end %>