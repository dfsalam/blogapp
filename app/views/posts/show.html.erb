<h2>Welcome to posts show page</h2>
<section>
  <div>
    <h4>Post #1</h4>
    <div>
      <h4>Comments: <%= @post.comments_counter%></h4>
      <h4>Likes: <%= @post.likes_counter%></h4>
    </div>
  </div>
  <p><%= @post.text%> </p>  
</section>
<section>  
  <% @comments.each do |comment| %>
    <% user = User.find(comment.author_id) %>
    <h3><%= user.name %>: <%= comment.text%></h3>
  <% end %>
</section>